---
import Layout from '../layouts/Layout.astro';

const { message } = Astro.url.searchParams;
---

<Layout title="Error - Dev Docs">
  <div class="max-w-2xl mx-auto text-center">
    <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-4">
      Oops! Something went wrong
    </h1>
    <div class="bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-lg p-6 mb-6">
      <p class="text-red-800 dark:text-red-200 mb-4">
        {message || 'An unexpected error occurred while fetching the repository data.'}
      </p>
      <p class="text-sm text-red-600 dark:text-red-400">
        This might be because:
      </p>
      <ul class="text-sm text-red-600 dark:text-red-400 list-disc list-inside mt-2">
        <li>The repository doesn't exist or is private</li>
        <li>GitHub API rate limits have been exceeded</li>
        <li>The repository name is incorrect</li>
      </ul>
    </div>
    <a
      href="/projects"
      class="inline-block px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors"
    >
      Back to Projects
    </a>
  </div>
</Layout> 